# Initial experiments (archive)

Earlier exploration (triplet variants, SmallResNet hybrids, curriculum). Not the
paper table.

Paper configs live in `configs/` (see the root README).

```bash
PYTHONPATH=. python train.py --config configs/initial_exps/<name>.json --device cuda:0
```
